import './home.scss';
import React from 'react';
import PropTypes from 'prop-types';

Home.propTypes = {
    
};

function Home(props) {
    return (
        <div className="grid wide home-page">
            home
        </div>
    );
}

export default Home;